import { TableTextTruncatePipe } from './table-text-truncate.pipe';

describe('TableTextTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new TableTextTruncatePipe();
    expect(pipe).toBeTruthy();
  });
});
